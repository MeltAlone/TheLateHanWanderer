# .NET 纵向切片技术尖峰

> 实现版本：`0.8.0-spike`<br>
> SDK：.NET 10.0.302<br>
> 日期：2026-07-24

## 1. 已打通链路

```text
场景 JSON
-> 结构化反序列化与跨文件引用校验
-> 规范化内容哈希
-> 纯领域 WorldState
-> CLI 查询/移动/交付/陈述/等待
-> 不可变事件与直接因果边
-> 稳定分阶段未来事件队列与精确等待中断
-> 版本化、按领域隔离的 xoshiro256** 随机流
-> 持久行动实例、路段位置与分阶段旅行
-> 旅行中断、恢复、取消和行动中存档
-> L0-L3 精度状态与 L3 群体成员确定性升降格
-> 快照保存/加载
-> 相同输入的事件指纹比较
```

实际玩家路径为：沈循从大将军府步行至司隶校尉署，向袁绍交付封记，返回大将军府向李文回报。总耗时 54 分钟，产生 11 个按序事件并关闭两项承诺；回报包含消息创建、送达和信念更新。

## 2. 解决方案边界

| 项目 | 当前职责 | 允许依赖 |
|---|---|---|
| `LateHan.Core` | 世界状态、路径、行动、调度、随机流、事件和确定性指纹 | .NET BCL |
| `LateHan.Scenarios` | JSON DTO、数据验证、规范化哈希和领域构建 | Core |
| `LateHan.Persistence` | 版本化快照 DTO 和原子文件写入 | Core |
| `LateHan.Cli` | 命令解析和中文文本呈现 | Core、Scenarios、Persistence |
| `LateHan.Tests` | 行为、确定性和快照测试 | Core、Scenarios、Persistence |
| `LateHan.Benchmarks` | 无界面重复运行和指纹检查 | Core、Scenarios |

核心项目没有引用 `System.Text.Json`、文件路径、数据库或 CLI 文本。当前最短路径按整数分钟计算，并用稳定 ID 处理相同成本的排序。

## 3. 当前验证结果

参考环境：Windows 11 10.0.22621、Intel Core i5-12400F（6 核 12 线程）、15.9 GiB 内存、.NET 10.0.302，Release 构建。

| 检查 | 结果 |
|---|---|
| Release build | 0 警告，0 错误 |
| 自动测试 | 64/64 通过 |
| 场景 PowerShell 校验 | 通过 |
| C# 场景加载与引用校验 | 通过 |
| 规范化内容哈希 | `sha256:0a004bb8ec5eecf37aa66dd813570922e2a4a967fe7176cd4d2af7c2c2a78caf` |
| 完整送信事件指纹 | `sha256:d6a6366d1f96f3e8f6938cdadc69c7c38d7fe028ced1eac13b8de44223db99fa` |
| 100 次加载并交付 | 总计 617.53 ms，均值 6.175 ms；指纹 `sha256:31935294...ff10afc` |
| B1 缩小版：379 人口当量空闲 30 日 | 10.59 ms，处理 719 个实际事件；指纹 `sha256:cdd92055...cca51151` |
| B4 结构子集：1,000 次升降格往返 | 23.31 ms，分配 6,217,456 bytes；人口和物质状态指纹守恒 |
| B2 缩小版：12 名城内人物并发行动 | 2.27 ms，86 个事件、24 人次精度评估；人口守恒，指纹 `sha256:e4b23280...118319` |
| B3 缩小版：3 个命题沿 14 名载体传播 | 5.20 ms，42 条消息、39 条父链接；指纹 `sha256:eb35f890...11e2db` |
| B2 目标人口核心：50 L0 + 500 L1 + 200 万 L3 人口当量 | 优化后 5 样本中位数 175.917 ms、p95 184.432 ms；540 个并发旅行者、15,502 个事件、人口守恒，指纹 `sha256:c3276c53...8a452f` |
| B2 混合结构：目标人口、访问、消息、计划、远方批处理与玩家中断 | 最近一次独立 5 样本中位数 145.180 ms、p95 164.583 ms；玩家中断推进 p95 38.307 ms，15,743 个事件，指纹 `sha256:333c83cb...ab6116` |
| B3 目标消息拓扑：50 地点、200 载体 | 最近一次独立 5 样本中位数 15.904 ms、p95 16.818 ms；200 条消息、150 条父链接，指纹 `sha256:7a62a68b...c4c03` |
| B3 冲突语义：50 地点、200 载体 | 最近一次独立 5 样本中位数 42.364 ms、p95 44.657 ms；300 条消息、100 次失真、100 个冲突持有人，指纹 `sha256:f560152a...24572` |

缩小负载是单次校准；四个目标结构负载各执行 1 次热身和 5 次全新世界样本，事件指纹审计单独计时。B2 目标人口核心和混合结构处于 `< 250 ms` 预算内，混合负载已具备可守恒的 L2/L3 远方批处理，但仍缺拒绝/排队访问和计划资源冲突；B3 已分别用纯拓扑与冲突语义负载验证真实消息网络、版本化转述失真和冲突认知。这些结果仍不能替代完整 B1-B6。完整边界见[P6 精度与规模报告](p6-scale-report.md)。

## 4. 已实现的兼容性约束

- 场景哈希忽略对象键顺序、无意义空白以及清单自身的 `content_hash` 字段。
- 世界状态保存场景版本、规则版本、RNG 版本、引擎版本和内容哈希。
- 快照使用稳定 DTO，不序列化领域对象图。
- 快照先写临时文件再原子替换目标文件。
- 当前尖峰只接受完全匹配的引擎版本；正式迁移器尚未实现。
- 无效移动在验证阶段失败，不推进时间，也不产生事件。
- 同刻队列使用 `(due_minute, phase, stable_subject_id, sequence)`，整批结算后才暂停。
- 快照 schema `0.9` 在既有认知传播状态之外，保存远方群体粮食账本、余数、结算游标、未来 tick，以及 L2 人物背景周期和原因事件。
- `dev rng` 只在副本上预览；`dev schedule` 留下外部干预事件并标记回放。
- 旅行使用稳定行动 ID 和逐路段完成事件；中断保存整数进度，恢复可更换交通方式。
- 运行中和中断行动均可保存；加载后的完成状态、事件指纹、队列和随机游标与连续运行一致。

## 5. 已实现的 P4 闭环

- 场景信念和计划加载为运行时状态；初始复评事件进入统一调度队列。
- 正式报告交付后产生 `belief_updated`，计划在同一分钟复评并调用通用旅行行动。
- NPC 旅行完成产生 `plan_completed`，事件原因链连接报告、信念、复评、旅行和抵达。
- 快照保存并恢复计划挂起事件、关联行动和信念来源，续跑与连续运行保持相同指纹。

## 6. 已实现的 P5 闭环

- 场景访问规则、人物组织身份、凭证范围与有效期、地点开闭状态映射为纯领域对象。
- `enter` 验证邻接关系并消耗五分钟通报时间；允许、拒绝和中断均留下事件与原因。
- `BeginTravel` 和抵达结算复用门禁判定；同刻 `access_and_control_change` 可以阻止随后发生的抵达。
- 口头陈述形成可持久化消息，接收者信念引用送达事件，转述消息保留父消息 ID。
- 交付和交谈在耗时期间处理世界事件；接收者离开时不产生跨空间转移或认知更新。

## 7. 已实现的 P6 精度基础

- 具名人物使用 L0-L2，人口群体固定为 L3；L3 不展开为海量个人对象。
- 群体升格使用稳定单调 ID 和由场景内容派生的身份种子，不依赖容器遍历顺序或普通随机流。
- 升格继承群体可知信念；独立消息、信念、持有物、承诺、计划、行动或位置偏离都会阻止降格。
- 1,000 次干净升降格往返保持总人口和物质状态指纹不变；升格与降格事件仍保留在因果日志中。
- CLI 提供 `groups`、`detail`、`dev promote` 和 `dev demote`，快照可恢复临时人物身份与人口游标。
- `attention-and-causal-debt.v1` 按玩家同地/相邻、活动行动与计划、开放承诺和七日内消息评估 L0-L2，并将每次变化的理由写入事件。
- `dev rebalance-detail` 可显式重平衡全世界或单个人物；评估按稳定人物 ID 排序，不依赖字典遍历顺序。
- benchmark 专用合成工厂可以稳定创建 50 个 L0、500 个 L1、200 万 L3 人口当量、50 地点和 200 消息载体，不污染历史场景内容。
- `b2-scale` 验证 540 个并发旅行行动、增量脏集精确消费、人口守恒和 L3 不展开；`b3-scale` 验证 200 条消息、150 条父链接和逐接收者消息来源信念。
- `b2-mixed` 在相同目标人口上验证两次玩家中断和恢复、20 次访问往返、40 条消息、10 个计划完成和 3 个远方 tick，并单独报告推进至玩家中断的延迟。
- 命题以 `topic_id` 和 `stance` 表达同一议题上的竞争判断；旧场景默认一命题一议题且不失真。
- `retelling-distortion.v1` 按消息稳定 ID 使用隔离随机流，保存发送者原始命题、实际送达命题、失真抽样、置信度衰减和父消息。
- 同一人物对同一议题持有不同立场时派生冲突认知，首次形成时产生 `belief_conflict_detected`；计划和认知查询仍不读取世界真相。
- `b3-conflict` 验证 300 条消息、100 次可审计失真、100 个冲突持有人、精确冲突事件数量和五次确定性指纹。
- `distant-batch.v1` 按低频事件结算 L3 生产、需求、消费、库存和未满足量；整数余数结转保证分批等价，人数变化前先切账，L3 不展开。
- 同地、同组织的 L2 具名人物共享一个批量原因事件并推进背景周期；自动排程幂等，结算后只续排一个未来 tick。
- B2 混合负载的三次批处理满足 `500万期初 + 90万生产 = 100万消费 + 490万期末`，人口当量保持 2,000,550。
- 目标结构负载使用 1 次热身和 5 次样本，分别报告构造与模拟时间、分配、样本工作集、GC 和确定性指纹。
- `WorldState` 在构造或快照加载时重建稳定路线遍历索引和人物行动索引；查询成本不再随全图路线或全世界行动线性扫描。
- 事件与调度详情只复制为一次有序只读快照；事件指纹使用低分配 UTF-8 缓冲，并以发布过的完整送信指纹自动测试锁定协议。
- benchmark 将模拟本体与事件指纹审计分开计量；B2 模拟分配中位数由约 98.4 MB 降至约 45.1 MB，指纹保持不变。
- 人物位置更新同时维护地点/路段空间索引；玩家移动只令旧、新注意空间及其邻接地点失效，普通人物只令自身失效。
- 行动、计划、承诺、消息和消息保留期到期均会增量标脏；脏集、认知传播和远方批处理状态进入 schema `0.9` 快照，加载后空间索引由位置重建。
- 精度刷新保持显式边界，不改变原有旅行、访问、交付和交谈动作的核心事件序列。

## 8. 尚未实现

- 多 NPC 计划协调、资源竞争和更复杂的计划复评；
- 群体传言升格、自然语言文本生成、主动查证、来源信誉和复杂置信度合成；
- 门禁请求、交付和陈述像旅行一样保存于行动中途的通用持久行动协议；
- 正式客户端和世界调度器采用何种稳定边界自动消费精度脏集；
- 事件溯源投影、增量快照和存档迁移器；
- 汉历时辰显示、疲劳、马匹授权和路况扰动；
- B2 拒绝/排队访问和计划资源冲突；
- 正式 B1/B4，以及 B5、B6 合成世界性能基准。

下一实现阶段应优先补齐 B2 的竞争访问和计划资源冲突，再用长期日志与分叉负载决定事件持久化架构；主动查证、来源信誉与冲突消解作为后续 B3 增强，不能先增加图形界面。

# 数据目录

`data/` 只保存机器可读或准备进入机器可读流程的数据，不保存大段设计说明。

- `historical/`：有来源的历史主张。允许互相冲突，但每条必须带来源和置信度。
- `scenarios/`：模拟可直接加载的完整初始状态。未知字段必须使用显式假设或生成规则补齐。
- `generated/`：本地生成产物，不进入 Git。

史料原文、现代地图和其他可能受版权限制的材料不直接复制进仓库；仓库保存书目信息、允许范围内的短引文、研究笔记和可复核链接。

